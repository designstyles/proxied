name: proxied
description: A small middleware package for wrapping api logic in a uniform object response.
version: 1.0.2
repository: https://github.com/designstyles/proxied

environment:
  sdk: ^3.5.0

# Add regular dependencies here.
dependencies:
  freezed_annotation: ^2.4.1
  freezed: ^2.4.6

dev_dependencies:
  build_runner: ^2.4.6
  lints: ^4.0.0
  test: ^1.24.0
